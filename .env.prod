# 织镜 ZHÌJÌNG — 生产环境配置
# APP_ENV=production 时自动加载（覆盖 .env 基础值）
# 此文件提交到仓库，不要放真实密钥，生产部署通过环境变量注入

# ============================================================
# LLM（核心依赖，必填）
# ============================================================
LLM_API_KEY=your-api-key

# ============================================================
# 搜索（可选，不配则 LLM 基于自身知识输出）
# ============================================================
SEARCH_API_KEY=your-search-api-key
SEARCH_PROVIDER=auto

# ============================================================
# 文生图（可选）
# ============================================================
ARK_API_KEY=your-ark-key

# ============================================================
# 环境 / 运行
# ============================================================
APP_ENV=production
LOG_LEVEL=warning
REQUEST_TIMEOUT=120
API_TOKEN=change-me-to-a-random-string
